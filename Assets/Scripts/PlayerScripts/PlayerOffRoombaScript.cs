using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerOffRoombaScript : MonoBehaviour
{/*
  //When player armature is triggered by sphere at butt of roomba,
  //called "OffButton", visual indicator of which button to press (e),
  // if player presses e then roomba will be turned off
  //should this be moved to the roombas themselves?-moved
  private bool _turnOff;

    // Start is called before the first frame update
    void Start()
    {
      TurnedOff(false);
    }

    // Update is called once per frame
    void Update()
    {
      if(_turnOff) //if in range of off button
      {
        Input.KeyCode //give choice of turning off with E
      }
    }
    public void OnTriggerEnter(Collider other) //if trigger off button
    {
      if(other.CompareTag("OffButton"))
      {
        _turnOff = true;
      }
    }
    public void OnTriggerExit(Collider other) //if no longer trigger off button
    {
      if(other.CompareTag("OffButton"))
      {
        _turnOff = false;
      }
    }
    private void TurnedOff(bool button)//set to off when game starts
    {
      _turnOff = button;
    }*/
}
